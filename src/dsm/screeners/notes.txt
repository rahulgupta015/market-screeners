
+----------------------+---------------------------------------------------------------+
|       ZONE          |                         CONDITIONS                            |
+----------------------+---------------------------------------------------------------+
|  BULLISH            | CMP > 50 DMA > 100 DMA > 200 DMA                              |
|                     | AND CMP > 1.10 × 200 DMA                                      |
|                     | → Fully stacked, price well above long-term trend             |
+----------------------+---------------------------------------------------------------+
|  ENTERING BULLISH   | CMP > 50 DMA > 100 DMA > 200 DMA                              |
|                     | AND 1.00 × 200 DMA < CMP ≤ 1.10 × 200 DMA                     |
|                     | → Structure bullish but price just breaking above 200 DMA     |
+----------------------+---------------------------------------------------------------+
|  UNCONFIRMED        | CMP between 0.90 × 200 DMA and 1.00 × 200 DMA                 |
|                     | OR moving averages not stacked cleanly                        |
|                     | → Neutral zone, trend unclear                                 |
+----------------------+---------------------------------------------------------------+
|  ENTERING BEARISH   | CMP < 50 DMA < 100 DMA < 200 DMA                              |
|                     | AND 0.90 × 200 DMA ≤ CMP < 1.00 × 200 DMA                     |
|                     | → Structure bearish but price hovering near 200 DMA           |
+----------------------+---------------------------------------------------------------+
|  BEARISH            | CMP < 50 DMA < 100 DMA < 200 DMA                              |
|                     | AND CMP < 0.90 × 200 DMA                                      |
|                     | → Fully stacked bearish, price well below long-term trend     |
+----------------------+---------------------------------------------------------------+

Days since 52W low only , positive if its after 52W high, negative otherwise. Green dot when positive

DMA Alignment breakout -> ENTERING BULLISH and car score >= 1 -> green check
CAR breakout ->  CMP > 50 DMA & CMP > 100 DMA & CMP > 200 DMA and CMP within 0.1 to 10% of 200 DMA and car score >=7 -> fluorescent green check

Every stock that will be processed. We do not skip any stock.
We do not print data for a stock that can not be calculated based on historical data or lack of hiostroical data. We simplly keep it blank.
So print whatever u can and leave as blank what can not be calculated.
Print the current date on top of the below table.
Also fetch the market cap in Billions of the stock from y-finance.



Columns will be as below. Sort by ticker.

Stock, market cap, CMP, DMA Alignment BO, CAR BO, 30 DMA, 50 DMA, 100 DMA, 200 DMA, Shift % From 200 DMA, CAR, Zone, 52W High, 52W Low, Days since 52W low

Document all the logic above in readme file


DMA Alignment BO -> green check mark only, leave blank otherwise
CAR BO -> florescent green check mark, leave blank otherwise
30 DMA, 50 DMA, 100 DMA, 200 DMA -> display value and display only green dot
Shift % From 200 DMA -> display value and green when between light purple when > 10, green when between 0.1 to 10 , yellow when between -10 to 0.1, red otherwise
CAR -> Car score < = 1 -> red , 2 <= score <= 4 florescent orange, 5 <= score <= 7 -> yellow , >= 8-> green
Zone-> Print from the table and u can use red, orange, yellow, green and purple
Days since 52W low -> green dot if its positive


Have a method to calculate and return CAR
Have a method to return Zone by passing in the DMAs, shift % and car
Have a method to print the data
All of this will make the code simple

